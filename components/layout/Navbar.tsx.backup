'use client'

import Link from 'next/link'
import Image from 'next/image'
import { useState } from 'react'
import { Menu, X, PlusCircle, User } from 'lucide-react'

export function Navbar() {
  const [isOpen, setIsOpen] = useState(false)

  return (
    <nav className="sticky top-0 z-50 bg-white/95 backdrop-blur-sm border-b border-[#E9ECEF]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-16">
          {/* Logo */}
          <Link href="/" className="flex items-center gap-2.5 group">
            <Image
              src="/logo.png"
              alt="Huts"
              width={36}
              height={36}
              priority
              className="h-9 w-9 object-contain"
            />
            <span className="text-xl font-bold tracking-tight text-[#212529]">HUTS</span>
          </Link>

          {/* Desktop Navigation */}
          <div className="hidden md:flex items-center gap-8">
            <Link
              href="/search"
              className="text-sm font-medium text-[#495057] hover:text-[#212529] transition-colors"
            >
              Browse
            </Link>
            <Link
              href="/areas"
              className="text-sm font-medium text-[#495057] hover:text-[#212529] transition-colors"
            >
              Areas
            </Link>
            <div className="flex items-center gap-3 ml-2">
              <Link
                href="/dashboard"
                className="inline-flex items-center gap-1.5 text-sm font-medium text-[#212529] border border-[#E9ECEF] px-4 py-2 rounded-md hover:border-[#212529] transition-colors"
              >
                <User size={16} />
                Sign In
              </Link>
              <Link
                href="/dashboard/new-property"
                className="inline-flex items-center gap-1.5 text-sm font-medium text-white bg-[#212529] px-4 py-2 rounded-md hover:bg-black transition-colors"
              >
                <PlusCircle size={16} />
                Post Property
              </Link>
            </div>
          </div>

          {/* Mobile Menu Button */}
          <button
            onClick={() => setIsOpen(!isOpen)}
            className="md:hidden p-2 rounded-md hover:bg-[#F8F9FA] min-w-[44px] min-h-[44px] flex items-center justify-center"
            aria-label="Toggle menu"
          >
            {isOpen ? <X size={24} /> : <Menu size={24} />}
          </button>
        </div>

        {/* Mobile Navigation */}
        {isOpen && (
          <div className="md:hidden pb-4 space-y-1 border-t border-[#E9ECEF] pt-4">
            <Link
              href="/search"
              className="block px-4 py-3 rounded-md text-[#212529] font-medium hover:bg-[#F8F9FA] min-h-[44px]"
              onClick={() => setIsOpen(false)}
            >
              Browse Properties
            </Link>
            <Link
              href="/areas"
              className="block px-4 py-3 rounded-md text-[#212529] font-medium hover:bg-[#F8F9FA] min-h-[44px]"
              onClick={() => setIsOpen(false)}
            >
              Area Guides
            </Link>
            <Link
              href="/dashboard"
              className="block px-4 py-3 rounded-md text-[#212529] font-medium hover:bg-[#F8F9FA] min-h-[44px]"
              onClick={() => setIsOpen(false)}
            >
              Sign In
            </Link>
            <div className="pt-2 px-4">
              <Link
                href="/dashboard/new-property"
                className="block w-full text-center py-3 bg-[#212529] text-white rounded-md font-medium hover:bg-black min-h-[44px]"
                onClick={() => setIsOpen(false)}
              >
                Post Property
              </Link>
            </div>
          </div>
        )}
      </div>
    </nav>
  )
}
